# Study Buddy - Production Requirements
# This file contains all dependencies needed to run Study Buddy in production.
# All versions are pinned for reproducible builds and security.

# Core MCP Framework - Model Context Protocol support
mcp>=1.0.0,<2.0.0

# Document Processing Dependencies
PyPDF2==3.0.1          # PDF document parsing and text extraction
python-docx==1.1.0      # Microsoft Word document processing
python-pptx==0.6.23     # Microsoft PowerPoint presentation processing  
markdown==3.5.2         # Markdown file processing and HTML conversion

# Data Validation and Serialization
pydantic==2.8.2         # Data validation and settings management
pydantic-settings==2.4.0 # Settings management with Pydantic

# Async Support and Networking
aiohttp==3.9.1          # Async HTTP client/server for MCP communication
aiofiles==23.2.0        # Async file operations
httpx==0.27.0           # Modern async HTTP client

# Database Support (SQLite built-in, but these for advanced features)
sqlalchemy==2.0.25      # SQL toolkit and ORM (optional advanced features)

# GUI Framework (tkinter is built-in, these are for enhanced features)
pillow==10.2.0          # Image processing for thumbnails and icons
tkinterdnd2==0.3.0      # Drag and drop support for tkinter

# Text Processing and NLP
nltk==3.8.1             # Natural language processing toolkit
spacy==3.7.2            # Advanced NLP library (optional)

# Utilities and Tools
python-magic==0.4.27    # File type detection
chardet==5.2.0          # Character encoding detection
python-dateutil==2.8.2  # Date parsing utilities
pytz==2024.1            # Timezone handling

# Logging and Monitoring
structlog==24.1.0       # Structured logging
psutil==5.9.8           # System and process monitoring

# Security and Encryption
cryptography==42.0.2    # Cryptographic functions for secure storage
bcrypt==4.1.2           # Password hashing (for future auth features)

# Configuration Management  
pyyaml==6.0.1           # YAML configuration file support
toml==0.10.2            # TOML configuration file support
configparser==6.0.0     # INI configuration file support

# Development Quality Tools (optional, but recommended)
# These can be installed separately with: pip install study-buddy[dev]

# Testing Framework
# pytest==7.4.4
# pytest-cov==4.1.0
# pytest-asyncio==0.23.2
# pytest-mock==3.12.0

# Code Formatting and Linting
# black==24.1.1
# ruff==0.1.11
# mypy==1.8.0
# types-requests==2.31.0.20240125
# types-pyyaml==6.0.12.12

# Documentation
# sphinx==7.2.6
# sphinx-rtd-theme==2.0.0
# myst-parser==2.0.0

# Build Tools
# build==1.0.3
# wheel==0.42.0
# setuptools==69.0.3

# Platform-specific Dependencies
# Windows
pywin32==306; sys_platform == "win32"
pywin32-ctypes==0.2.2; sys_platform == "win32"

# macOS  
pyobjc-framework-Cocoa==10.1; sys_platform == "darwin"
pyobjc-framework-Quartz==10.1; sys_platform == "darwin"

# Linux
# Most Linux dependencies are system-level packages
# Refer to installation documentation for distribution-specific requirements

# Optional Performance Enhancements
# These can improve performance but are not required
# orjson==3.9.12         # Faster JSON serialization
# uvloop==0.19.0         # Faster event loop (Unix only)
# ujson==5.9.0           # Ultra-fast JSON parsing

# Version Constraints Summary:
# - All major versions are pinned to prevent breaking changes
# - Minor version ranges allow security updates
# - Platform-specific packages only install where needed
# - Development dependencies are commented out (install with [dev] extra)

# Installation Commands:
# Standard installation:     pip install -r requirements.txt
# Development installation:  pip install -r requirements.txt -r requirements-dev.txt
# Full installation:         pip install study-buddy[all]