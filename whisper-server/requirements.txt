# Whisper MCP Server Requirements for Docker with CUDA
# Install with: pip install -r requirements.txt

# Core MCP package
# Note: MCP package may not be publicly available yet
# The server includes fallback mode for development
mcp>=1.0.0

# PyTorch with CUDA support (installed separately in Dockerfile)
# torch>=2.0.0
# torchaudio>=2.0.0

# Hugging Face Transformers for local Whisper model (compatible with PyTorch 2.4.1)
transformers==4.44.2
datasets==2.20.0
accelerate==0.34.2

# Audio processing
librosa==0.10.2
soundfile==0.12.1

# Audio/Video conversion (optional but recommended)
ffmpeg-python==0.2.0

# Async processing for parallel operations
aiofiles==24.1.0

# Async HTTP server
aiohttp==3.10.5

# FastAPI for HTTP API mode
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Environment variables
python-dotenv>=1.0.0

# For development without MCP package, the server works in fallback mode
# All functionality is preserved through fallback implementations